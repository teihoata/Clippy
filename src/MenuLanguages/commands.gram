#JSGF V1.0;

/**
 * JSGF Grammar for standard commands that will spawn in each menu option
 */

grammar commands;

public <standard_commands> =
                      hide clippy   { hide } |
                      main menu    { goto_menu } ;
